# Release builds (e.g. Render): API base is set at compile time via
# --dart-define=API_BASE_URL=... in Dockerfile.web. Keys here are optional
# overrides for other env-driven features.

API_BASE_URL=
